services:
  solar-pro:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: solar-pro-app
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
